#include <stdo.h>
/**
 * main -A programe that print a line unist the printf function
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n")
		return (0);
}
